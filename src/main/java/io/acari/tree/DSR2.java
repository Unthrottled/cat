package io.acari.tree;

import io.acari.tree.pojo.Node;

import java.util.stream.Stream;

public class DSR2 {

    public Stream<String> find(Node root, int size){
        return Stream.empty();
    }
}
